# システム仕様

本ドキュメントは、コードから自明でないシステム挙動を補足するための仕様書です。

## 自動アップデート

`electron-updater` (`autoUpdater`) を用いて GitHub Releases から差分配信を受け取る。

### 起動シーケンス

1. `app.whenReady` 直後に `initializeUpdater()` を呼び、`autoUpdater` のイベントハンドラを登録する。
2. メインウィンドウ生成後に `scheduleStartupCheck(mainWindow)` を呼ぶ。
   - `webContents` がまだロード中であれば `did-finish-load` を待ってから 3 秒後に `checkForUpdates()` を実行する。
   - すでにロード済みなら 3 秒の遅延だけを挟む。
   - 起動チェックは 1 度のみ実行する (内部フラグで二重起動を抑止)。

### 開発モードでは完全 no-op

`process.env.NODE_ENV === 'development'` または `--dev` 引数が付与されている場合、初期化・チェック・ダウンロード・インストール・スケジューリングはすべて早期 return する。`yarn dev` で GitHub への通信が一切発生しないことを保証する。

### Windows portable ビルドでも完全 no-op

`process.env.PORTABLE_EXECUTABLE_FILE` が定義されている場合 (electron-builder が portable target で起動した実行ファイルにのみ自動付与する環境変数)、`initializeUpdater` / `checkForUpdates` / `downloadUpdate` / `quitAndInstall` / `scheduleStartupCheck` はすべて早期 return する。

理由: portable 版で `electron-updater` が動作すると `latest.yml` に従って NSIS 版インストーラーをダウンロード・実行し、ユーザーが意図しない場所に通常版がインストールされてしまうため。

NSIS 版で起動した場合はこの環境変数が定義されないため、自動更新は従来どおり機能する。

### `autoUpdater` 設定

| プロパティ | 値 | 理由 |
|---|---|---|
| `autoDownload` | `false` | ユーザー確認後にのみダウンロードを開始するため |
| `autoInstallOnAppQuit` | `false` | アプリ終了時の意図しない自動再起動を避けるため |
| `logger` | `console` | console-bridge を経由して DevTools に流すため |

### イベントとブロードキャスト方針

| イベント | 内部 state | renderer へのブロードキャスト |
|---|---|---|
| `checking-for-update` | `checking` | しない (UI 無音) |
| `update-available` | `available` | する |
| `update-not-available` | `not-available` | しない (UI 無音) |
| `download-progress` | `downloading` + `progress` | する |
| `update-downloaded` | `downloaded` | する。`downloadRequested` を false に戻し、`autoInstallOnDownloaded` フラグが true なら 1.5 秒後に `quitAndInstall` を自動実行 |
| `error` | `downloadRequested === true` のとき `error` (メッセージ付き)、それ以外は `idle` (リセット) | `downloadRequested === true` のときのみ `error` 状態をブロードキャストして UI に再試行/閉じるを提示する。それ以外 (起動時/バックグラウンドのチェック失敗、オフライン等) は `idle` に戻すだけで UI 無音。いずれも `autoInstallOnDownloaded` を false に戻す |

#### `error` ハンドラの分岐 (`downloadRequested` フラグ)

`autoUpdater.on('error')` はグローバルで、起動時/バックグラウンドのチェック失敗もユーザー操作によるダウンロード失敗も同じハンドラに届く。両者を区別するため `downloadRequested` フラグを導入する。

- `downloadUpdate()` 開始時に `true`、`update-downloaded` (完了) / `error` (失敗) / `downloadUpdate()` の reject 時に `false` に戻す。
- `error` ハンドラはこのフラグが `true` のときだけ `error` 状態 (失敗メッセージ付き) をブロードキャストする。これにより「ユーザーがアップデートを押してダウンロード中の失敗」だけ UI にエラーを表示し、オフライン等の起動時チェック失敗は静かに `idle` へ戻す。

### 主要 API

- `downloadUpdate()`: `autoInstallOnDownloaded = true` と `downloadRequested = true` をセットし、即座に `downloading` (progress 0) をブロードキャストしてから `autoUpdater.downloadUpdate()` を呼ぶ。これによりユーザー操作後は必ずフィードバック (プログレスバー) が出る。reject された場合で、かつグローバル `error` ハンドラがまだ処理していなければ (`downloadRequested` が true のまま) `error` 状態をブロードキャストする。
- `quitAndInstall()`: `setImmediate` で全ウィンドウを閉じてから `autoUpdater.quitAndInstall(false, true)` を呼ぶ。

### IPC チャンネル

| 定数 | チャンネル名 | 方向 |
|---|---|---|
| `UPDATER_GET_STATE` | `updater:getState` | renderer → main (invoke) |
| `UPDATER_CHECK` | `updater:check` | renderer → main (invoke) |
| `UPDATER_DOWNLOAD` | `updater:download` | renderer → main (invoke) |
| `UPDATER_QUIT_AND_INSTALL` | `updater:quitAndInstall` | renderer → main (invoke) |
| `UPDATER_STATE_CHANGED` | `updater:stateChanged` | main → renderer (push) |

### Renderer UI (`UpdateNotification`)

アプリのルート (常時マウントされる場所) に 1 つ配置する。

- `idle` / `checking` / `not-available` は何も描画しない。
- `available`: 「新しいバージョン v{{version}} が利用可能です。アップデートしますか？」を MUI Snackbar (右下、`autoHideDuration={null}`) で表示。「アップデート」「後で」の 2 ボタン。
- 「後で」を押すとそのセッション中は再表示しない (`dismissed`)。次回 `available` または `error` 状態に再遷移したときにリセット。
- 「アップデート」で `download()` を呼ぶ。以降の表示は:
  - `downloading`: 進捗バー (LinearProgress determinate + パーセント)
  - `downloaded`: 「アップデートを適用しています…」+ 不定形 LinearProgress
  - `error`: severity=error の Alert で失敗メッセージ (`state.error`) を表示し、「再試行」(`download()` 再実行) と「閉じる」(`dismissed`) の 2 ボタンを提示する。
- 購読は `subscribe → getState` の順とし、初期状態の取りこぼしを防ぐ。
- i18n キー: `updater.error` / `updater.retry` / `updater.close` を全対応言語 (ja / en) に追加。

### electron-builder の publish 設定

`electron-builder.yml`:

```yaml
publish:
  provider: github
  owner: kakehashi-inc
  repo: app_backup_restore
  releaseType: draft
```

- `repo:` はリポジトリ名のみ (URL ではない)。
- `releaseType: draft` のため、各プラットフォームでの `release:*` 実行は同一バージョンの 1 つの GitHub Draft Release に成果物を集約する。GitHub UI で "Publish release" を押すまでユーザーには配信されない。

### macOS のビルドターゲット (zip 必須)

macOS の自動更新 (Squirrel.Mac) は `.dmg` からは更新できず、`.zip` アーティファクトが必須である。`mac.target` が `dmg` のみだと `latest-mac.yml` がダウンロード可能なファイルを参照できず、`downloadUpdate()` が即失敗する。

このため `electron-builder.yml` の `mac.target` には `dmg` と `zip` を併存させ、それぞれ既存の `x64` / `arm64` 構成に合わせる。

```yaml
mac:
  target:
    - target: dmg
      arch: [x64, arm64]
    - target: zip
      arch: [x64, arm64]
```

- `.dmg` は手動インストール (初回配布) 用、`.zip` は自動更新用。両方を同じ Draft Release にアップロードする。
- 注意: この修正の効果が出るのは zip を含む新バージョンをリリースした後である。zip を含まない旧バージョンを使っている既存ユーザーは、`latest-mac.yml` が参照する zip を持たないため自動更新できず、一度だけ手動で新バージョン (zip を含むリリース) を入れる必要がある。それ以降は自動更新が機能する。

## Windows portable ビルドの zip 化リリース

未署名の `.exe` を直接配布するとブラウザやウイルス対策ソフトの警告が厳しくなるため、portable 版は `.zip` に圧縮した形で配布する。NSIS 版インストーラーはこの処理対象外。

### 仕組み

`electron-builder.yml` の `afterAllArtifactBuild: scripts/zip-portable.js` により、すべてのアーティファクトのビルド完了後に [scripts/zip-portable.js](../scripts/zip-portable.js) が呼び出される。フックは `BuildResult` を受け取り、生成した `.zip` のパス配列を返す (electron-builder が publish 対象に追加する)。

### 処理フロー

1. `buildResult.platformToTargets` を走査し、`platform.name === 'windows'` かつ `targets.has('portable')` の場合のみ後続処理に入る。macOS / Linux ビルドでは即 `[]` を返して no-op。
2. `buildResult.artifactPaths` から `.blockmap` のあるベース名を集合化する。
3. `.exe` のうち、同名の `.exe.blockmap` が集合に含まれないものを portable 版と判定する。NSIS 版は必ず `.exe` と `.exe.blockmap` のペアを出力するため、`.blockmap` のない `.exe` は確実に portable 版である。
4. 各 portable `.exe` を `archiver` (zip / level 9) で同名 `.zip` に圧縮し、圧縮成功後に元の `.exe` を `fs.rm` で削除する。
5. 生成した `.zip` パスの配列を返す。

### 影響範囲

- portable 以外の Windows アーティファクト (NSIS `.exe`, `.exe.blockmap`, `latest.yml`) は一切変更・削除しない。
- macOS / Linux ビルドでは早期 return するため、`afterAllArtifactBuild` の存在自体が他プラットフォームに影響しない。
